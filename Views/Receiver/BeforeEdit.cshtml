@model ASPNETAOP.Models.Data

@{
    ViewData["Title"] = "Edit Receiver";
}

<h1>Edit Receiver</h1>

<hr />
<div class="row">
    <div class="col-md-9">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            @{
                var ID = "@Model.Receiver.ID";
                var name = "@Model.Receiver.name";
                var listening_time = "@Model.Receiver.listening_time";
                var rest_time = "@Model.Receiver.rest_time";
                var recovery_time = "@Model.Receiver.recovery_time";
                <input type="hidden" id="@ID" name="@ID" value="@Model.Receiver.ID" />
                <input type="hidden" id="@name" name="@name" value="@Model.Receiver.name" />
                <input type="hidden" id="@listening_time" name="@listening_time" value="@Model.Receiver.listening_time" />
                <input type="hidden" id="@rest_time" name="@rest_time" value="@Model.Receiver.rest_time" />
                <input type="hidden" id="@recovery_time" name="@recovery_time" value="@Model.Receiver.recovery_time" />
                <div class="form-group">
                    <label asp-for="@Model.Receiver.ID" class="hidden"></label>
                    <input asp-for="@Model.Receiver.ID" class="hidden" type="hidden">
                    <span asp-validation-for="@Model.Receiver.ID" class="hidden"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Receiver.name" class="control-label"></label>
                    <input asp-for="@Model.Receiver.name" class="form-control" type="text" placeholder=@Model.Receiver.name/>
                    <span asp-validation-for="@Model.Receiver.name" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Receiver.listening_time" class="control-label"></label>
                    <input asp-for="@Model.Receiver.listening_time" class="form-control" type="number" placeholder=@Model.Receiver.listening_time step="0.01"/>
                    <span asp-validation-for="@Model.Receiver.listening_time" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Receiver.rest_time" class="control-label"></label>
                    <input asp-for="@Model.Receiver.rest_time" class="form-control" type="number" placeholder=@Model.Receiver.rest_time step="0.01"/>
                    <span asp-validation-for="@Model.Receiver.rest_time" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="@Model.Receiver.recovery_time" class="control-label"></label>
                    <input asp-for="@Model.Receiver.recovery_time" class="form-control" type="number" placeholder=@Model.Receiver.recovery_time step="0.01"/>
                    <span asp-validation-for="@Model.Receiver.recovery_time" class="text-danger"></span>
                </div>
            }
            <div class="form-group">
                <input type="submit" value="Edit" class="btn btn-primary" />
            </div>
            <b>@Model.message</b>
            @{ Model.message = null; }
        </form>
    </div>
</div>

<div>
    <input type="button" style="background-color:mediumblue; border-color:mediumblue; color:white; width:150px; height:40px;" title="Go Back" value="Go Back" onclick="location.href='@Url.Action("GoBack", "Receiver", new { id = @Model.Receiver.ID })'" />
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}